# Supabase project URL (safe to expose in the browser)
NEXT_PUBLIC_SUPABASE_URL=https://gitmyhrstoxjxqawsfbr.supabase.co

# Supabase anon key (safe to expose in the browser; never use service_role here)
NEXT_PUBLIC_SUPABASE_ANON_KEY=

# Optional: surface the environment name in the UI ("production" / "staging")
NEXT_PUBLIC_APP_ENV=staging
